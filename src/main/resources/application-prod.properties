spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:validate}

spring.kafka.bootstrap-servers=${SPRING_KAFKA_BOOTSTRAP_SERVERS}

spring.data.redis.host=${SPRING_DATA_REDIS_HOST}
spring.data.redis.port=${SPRING_DATA_REDIS_PORT:6379}

spring.mail.host=${SPRING_MAIL_HOST}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME}
spring.mail.password=${SPRING_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_SMTP_STARTTLS_ENABLE:true}

scheduler.storage.enabled=${SCHEDULER_STORAGE_ENABLED:true}
scheduler.storage.endpoint=${SCHEDULER_STORAGE_ENDPOINT}
scheduler.storage.access-key=${SCHEDULER_STORAGE_ACCESS_KEY}
scheduler.storage.secret-key=${SCHEDULER_STORAGE_SECRET_KEY}
scheduler.storage.bucket=${SCHEDULER_STORAGE_BUCKET:scheduler-workflow-data}
