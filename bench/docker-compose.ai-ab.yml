services:
  app:
    environment:
      WORKFLOW_AI_TOOL_CALLING_ENABLED: ${AI_AB_TOOL_CALLING_ENABLED:-true}
      WORKFLOW_AI_EMBEDDING_ENABLED: ${AI_AB_EMBEDDING_ENABLED:-true}
      WORKFLOW_AI_STRUCTURED_OUTPUT_ENABLED: ${AI_AB_STRUCTURED_OUTPUT_ENABLED:-true}
      WORKFLOW_AI_MAX_REPAIR_PASSES: ${AI_AB_MAX_REPAIR_PASSES:-2}
